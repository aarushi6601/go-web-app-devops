apiVersion: v1
kind: Service
metadata:
  name: my-service
  labels:
    app: go-web-app
spec:
  selector:
    app: go-web-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      ##container port inside which application is runnning
  type: NodePort
